{
  "id": "system-tools",
  "version": "0.1.4",
  "name": "System tools",
  "description": "Create a dev user and install base OS packages.",
  "options": {
    "username": {
      "type": "string",
      "default": "vscode",
      "description": "Username to create"
    },
    "userUid": {
      "type": "string",
      "default": "1000",
      "description": "User UID"
    },
    "userGid": {
      "type": "string",
      "default": "1000",
      "description": "User GID"
    },
    "packages": {
      "type": "string",
      "default": "",
      "description": "Additional apt packages to install (space/comma-separated). See src/system-tools/install.sh for the default package list."
    },
    "excludePackages": {
      "type": "string",
      "default": "",
      "description": "Apt packages to skip from the default install list (space/comma-separated). See src/system-tools/install.sh for the default package list."
    }
  }
}
